1.Install Python in your pc and make sure to add the path in your environment path  
2.Install Django in your pc in terminal 'python -m pip install Django'
3.go to the project path using the terminal cd RAITE2021/agency_management_system and type to terminal python manage.py runserver and coppy the ip link which is the http one.
